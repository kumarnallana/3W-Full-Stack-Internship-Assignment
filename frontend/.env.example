VITE_API_BASE_URL=http://localhost:5000/api
# real is the default and never falls back to local data after a network error.
# Set demo explicitly when you want the browser-only assessment walkthrough.
VITE_APP_MODE=real
